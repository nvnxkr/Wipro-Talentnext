def find_max_min_set():
    my_set = {15, 8, 32, 45, 3, 21}
    
    max_val = max(my_set)
    min_val = min(my_set)
    
    print(f"Set: {my_set}")
    print(f"Maximum value: {max_val}")
    print(f"Minimum value: {min_val}")

find_max_min_set()
